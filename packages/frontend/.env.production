# Automatisch geladen bij `npm run build` (Create React App).
# Lege API-URL = same-origin (/api, /socket.io) via nginx — standaard op allone.garden.
REACT_APP_API_URL=
REACT_APP_SERVER_NAME=AllOne Garden
