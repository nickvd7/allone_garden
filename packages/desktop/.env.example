#
# Desktop runtime optional integrations
# Copy to `.env` for local dev, or define via CI/runtime environment.
#

# Steamworks App ID (0 = disabled)
STEAM_APP_ID=0

# Discord Rich Presence App ID (0 = disabled)
DISCORD_APP_ID=0
